---
title: FloppyDisk.ts - A New Generation State Management Library for React
description: The Zustand-ReactQuery alternative for local, global, sync, or async state management.
keywords: Floppy Disk, React, State Management, JS, JavaScript, NPM, Store, TanStack-Query, Zustand, Alternative
---

import Banner from './_HomeBanner';
import Comparison from './_HomeComparison';

<Banner>

```bash
npm i floppy-disk
```

</Banner>

<Comparison />

<section className="mx-auto max-w-md pt-32 text-base sm:px-4 sm:pt-36 sm:text-lg md:px-0">
<div className="mx-auto text-center max-w-xs [&~ul]:mt-0">
## In Short
</div>

**Like Zustand, but has additional capabilities:**

- No selectors: automatically optimizes re-renders
- Store events: `onFirstSubscribe`, `onSubscribe`, `onUnsubscribe`, `onLastUnsubscribe`
- Easier to set initial state on SSR/SSG
- Smaller bundle

**Like TanStack Query, but:**

- DX is very similar to Zustand\
  → One mental model for sync & async
- Much smaller bundle than TanStack Query\
  → With nearly the same capabilities

</section>

<div className='h-28' />
